import React from 'react'

const Buttons = ({name}) => {
  return (
    <div > 
    <button className='px-1.5 py-1.5 m-2 bg-gray-200 rounded-lg'>{name}</button>
    
    </div>
  )
}

export default Buttons